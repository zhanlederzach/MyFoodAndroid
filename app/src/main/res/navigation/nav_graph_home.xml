<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_home"
    app:startDestination="@id/home_dest">

    <fragment
        android:id="@+id/home_dest"
        android:name="kz.myfood.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/goToHistoryDetails"
            app:destination="@+id/fragmentHistoryDetails"/>
    </fragment>

    <fragment
        android:id="@+id/fragmentHistoryDetails"
        android:name="kz.myfood.ui.home.todolist_types.HistoryDetails">
    </fragment>

</navigation>